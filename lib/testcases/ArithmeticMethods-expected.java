class ArithmeticMethods {
	int x = 69;
	int y = 420;
	int addX(int a) {
		return (x) + (a);
	}
	int addY(int a) {
		return (y) + (a);
	}
	int complexMath(int a, int b) {
		a = ((y) * ((a) + (b))) / (x);
		b = (a) + (-(b));
		a = (x) + ((b) * (a));
		return (x) * (a);
	}
}
